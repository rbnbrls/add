"use client";
import { useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function WhatsAppConnector(){const[sender,setSender]=useState("Alex"),[message,setMessage]=useState("Kun je hier vandaag op terugkomen?"),[messageId,setMessageId]=useState("whatsapp-demo-001"),[result,setResult]=useState("");async function intake(e:React.FormEvent){e.preventDefault();const r=await fetch(`${API}/api/connectors/whatsapp/intake`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sender,message,message_id:messageId,suggested_next_action:`Beantwoord WhatsApp-bericht van ${sender}`,confidence:.9})});setResult(r.ok?"WhatsApp-bericht als voorstel klaargezet voor Review.":"WhatsApp-intake mislukt.")}return <main className="security-shell"><header><span className="logo">ADD</span><span>WhatsApp instellen</span><a className="lab-link" href="/integrations">Integraties</a></header><section className="hero"><p className="eyebrow">WHATSAPP-WIZARD · INTAKE</p><h1>Een bericht binnenbrengen.</h1><p className="meta">Het bericht wordt alleen een voorstel. ADD stuurt nooit automatisch een antwoord.</p><form className="intake-form" onSubmit={intake}><label>Afzender<input autoFocus value={sender} onChange={e=>setSender(e.target.value)}/></label><label>Bericht<input value={message} onChange={e=>setMessage(e.target.value)}/></label><label>Message-ID<input value={messageId} onChange={e=>setMessageId(e.target.value)}/></label><button>Als voorstel klaarzetten</button></form><p className="message">{result}</p></section><p className="today-footer"><a href="/review">Open Review</a></p></main>}
