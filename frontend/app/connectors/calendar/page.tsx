"use client";
import { useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function CalendarConnector(){const[title,setTitle]=useState("Klantdemo voorbereiden"),[start,setStart]=useState("2026-09-15T10:00"),[location,setLocation]=useState("Online"),[eventId,setEventId]=useState("calendar-demo-001"),[result,setResult]=useState("");async function intake(e:React.FormEvent){e.preventDefault();const r=await fetch(`${API}/api/connectors/calendar/intake`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title,start,location,event_id:eventId,suggested_next_action:`Bereid voor: ${title}`,confidence:.9})});setResult(r.ok?"Agenda-item als voorstel klaargezet voor Review.":"Calendar-intake mislukt.")}return <main className="security-shell"><header><span className="logo">ADD</span><span>Calendar instellen</span><a className="lab-link" href="/integrations">Integraties</a></header><section className="hero"><p className="eyebrow">CALENDAR-WIZARD · INTAKE</p><h1>Een afspraak binnenbrengen.</h1><p className="meta">De starttijd wordt als deadline meegenomen. ADD maakt alleen een voorstel.</p><form className="intake-form" onSubmit={intake}><label>Titel<input autoFocus value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Starttijd<input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Locatie<input value={location} onChange={e=>setLocation(e.target.value)}/></label><label>Event-ID<input value={eventId} onChange={e=>setEventId(e.target.value)}/></label><button>Als voorstel klaarzetten</button></form><p className="message">{result}</p></section><p className="today-footer"><a href="/review">Open Review</a></p></main>}
