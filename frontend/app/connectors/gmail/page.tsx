"use client";
import { useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function GmailConnector(){const[subject,setSubject]=useState("Factuur maart"),[snippet,setSnippet]=useState("Kun je deze factuur controleren en verwerken?"),[messageId,setMessageId]=useState("gmail-demo-001"),[result,setResult]=useState("");async function intake(e:React.FormEvent){e.preventDefault();const r=await fetch(`${API}/api/connectors/gmail/intake`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subject,snippet,message_id:messageId,suggested_next_action:`Lees en verwerk: ${subject}`,confidence:.9})});setResult(r.ok?"Gmail-bericht als voorstel klaargezet voor Review.":"Gmail-intake mislukt.")}return <main className="security-shell"><header><span className="logo">ADD</span><span>Gmail instellen</span><a className="lab-link" href="/integrations">Integraties</a></header><section className="hero"><p className="eyebrow">GMAIL-WIZARD · INTAKE</p><h1>Een bericht binnenbrengen.</h1><p className="meta">Deze test maakt alleen een pending voorstel. ADD voert niets automatisch uit.</p><form className="intake-form" onSubmit={intake}><label>Onderwerp<input autoFocus value={subject} onChange={e=>setSubject(e.target.value)}/></label><label>Samenvatting<input value={snippet} onChange={e=>setSnippet(e.target.value)}/></label><label>Message-ID<input value={messageId} onChange={e=>setMessageId(e.target.value)}/></label><button>Als voorstel klaarzetten</button></form><p className="message">{result}</p></section><p className="today-footer"><a href="/review">Open Review</a></p></main>}
