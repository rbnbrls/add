"use client";
import { useEffect, useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
type Mode="shortcut"|"web_link"|"source"|"brain"|"voice_transcript";
export default function Intake(){const[mode,setMode]=useState<Mode>("shortcut"),[text,setText]=useState(""),[url,setUrl]=useState(""),[title,setTitle]=useState(""),[action,setAction]=useState(""),[source,setSource]=useState("gmail"),[reference,setReference]=useState(""),[deadline,setDeadline]=useState(""),[brainDump,setBrainDump]=useState(""),[saved,setSaved]=useState(false),[error,setError]=useState("");
 useEffect(()=>{const onShortcut=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.shiftKey&&event.key.toLowerCase()==="i"){event.preventDefault();setMode("shortcut");window.setTimeout(()=>{(document.querySelector('input[placeholder="bijv. Bel de tandarts"]') as HTMLInputElement|null)?.focus()},0)}};window.addEventListener("keydown",onShortcut);return()=>window.removeEventListener("keydown",onShortcut)},[]);
 async function submit(e:React.FormEvent){e.preventDefault();setError("");let endpoint="",payload:Record<string,unknown>={};if(mode==="brain"){endpoint=API+"/api/inbox/brain-dump";payload={text:brainDump}}else if(mode==="source"){endpoint=API+"/api/mcp/task-suggestions";payload={title,suggested_next_action:action,source_type:source,source_ref:reference||null,deadline:deadline||null,confidence:.8}}else if(mode==="web_link"){endpoint=API+"/api/inbox/capture";payload={text:title,source_type:"web_link",source_ref:url}}else{endpoint=API+"/api/inbox/capture";payload={text,source_type:mode==="voice_transcript"?"voice_transcript":"shortcut",source_ref:reference||null,timezone:"Europe/Amsterdam"}}const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});if(r.ok)setSaved(true);else setError((await r.json().catch(()=>null))?.detail||"Voorstel kon niet worden opgeslagen.")}
 if(saved)return <main className="intake-shell"><header><span className="logo">ADD</span><span>Inbox</span><a className="lab-link" href="/review">Review</a></header><section className="hero"><p className="eyebrow">OPGESLAGEN</p><h1>Voorstel staat klaar.</h1><p className="meta">De input staat in dezelfde reviewfunnel als e-mails, chatberichten en agenda-items.</p><a className="primary-link" href="/review">Open Review</a></section></main>;
 return <main className="intake-shell"><header><span className="logo">ADD</span><span>Inbox</span><a className="lab-link" href="/review">Review</a></header><section className="hero"><p className="eyebrow">CAPTURE FROM ANYWHERE</p><h1>Wat kwam er binnen?</h1><p className="meta">Elke ingang eindigt in dezelfde reviewfunnel. Planning wordt alleen als voorstel opgeslagen.</p><div className="actions"><button type="button" className={mode==="shortcut"?"":"secondary"} onClick={()=>setMode("shortcut")}>Snelle tekst</button><button type="button" className={mode==="web_link"?"":"secondary"} onClick={()=>setMode("web_link")}>Weblink</button><button type="button" className={mode==="voice_transcript"?"":"secondary"} onClick={()=>setMode("voice_transcript")}>Transcript</button><button type="button" className={mode==="source"?"":"secondary"} onClick={()=>setMode("source")}>Bronitem</button><button type="button" className={mode==="brain"?"":"secondary"} onClick={()=>setMode("brain")}>Braindump</button></div>{(mode==="shortcut"||mode==="voice_transcript")&&<form onSubmit={submit} className="intake-form"><label>{mode==="voice_transcript"?"Voice-transcript":"Snelle tekst"}<textarea required value={text} onChange={e=>setText(e.target.value)} placeholder={mode==="voice_transcript"?"Plak hier de transcriptie; audio wordt niet opgeslagen.":"bijv. Bel de tandarts"} autoFocus/></label>{mode==="voice_transcript"&&<label>Transcriptreferentie <span className="meta">(optioneel)</span><input value={reference} onChange={e=>setReference(e.target.value)} placeholder="bijv. voice:session-42"/></label>}<button>{mode==="voice_transcript"?"Transcript in review zetten":"In review zetten"}</button></form>}{mode==="web_link"&&<form onSubmit={submit} className="intake-form"><label>Titel of notitie<input required value={title} onChange={e=>setTitle(e.target.value)} placeholder="bijv. Lees dit artikel" autoFocus/></label><label>Weblink<input required type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://voorbeeld.nl/artikel"/></label><button>Weblink in review zetten</button></form>}{mode==="brain"&&<form onSubmit={submit} className="intake-form"><label>Braindump of vergadernotitie<textarea required value={brainDump} onChange={e=>setBrainDump(e.target.value)} placeholder="Zet alle losse punten hier neer…" autoFocus/></label><button>Voorstellen maken</button></form>}{mode==="source"&&<form onSubmit={submit} className="intake-form"><label>Bron<select value={source} onChange={e=>setSource(e.target.value)}><option value="gmail">Gmail</option><option value="calendar">Calendar</option><option value="whatsapp">WhatsApp</option><option value="home_assistant">Home Assistant</option></select></label><label>Titel<input required value={title} onChange={e=>setTitle(e.target.value)} placeholder="bijv. Reageren op verzekeraar" autoFocus/></label><label>Volgende actie<input required value={action} onChange={e=>setAction(e.target.value)} placeholder="bijv. Open het bericht"/></label><label>Deadline <span className="meta">(optioneel)</span><input type="datetime-local" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label><label>Bronreferentie <span className="meta">(optioneel)</span><input value={reference} onChange={e=>setReference(e.target.value)} placeholder="bijv. gmail:message-id"/></label><button>Voorstel klaarzetten</button></form>}<p className="message">{error}</p></section></main>}
