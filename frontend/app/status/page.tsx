"use client";
import { useEffect, useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
type Diagnostics={api:string;service:string;version:string;today:{ready:number;active:number;done:number;blocked:number;important_done:number;small_done:number};integrations:{ha_sync_configured:boolean;ha_context_configured:boolean;api_token_configured:boolean};deployment:{health:string;database:string;migration_revision:string|null;migration_ok:boolean}};
export default function Status(){const[data,setData]=useState<Diagnostics|null>(null),[error,setError]=useState("");async function load(){const r=await fetch(`${API}/api/diagnostics`);if(r.ok)setData(await r.json());else setError("Status kon niet worden geladen.")}useEffect(()=>{load()},[]);return <main><header><span className="logo">ADD</span><span>Status</span><a className="lab-link" href="/">← Terug naar NU</a></header><section className="lab-hero"><p className="eyebrow">OBSERVABILITY</p><h1>De systeemstatus.</h1><p className="meta">Veilige operationele informatie, zonder geheimen of credentials.</p><button className="secondary" onClick={load}>Vernieuwen</button></section>{data&&<><section className="lab-tools"><h2>API</h2><div className="pending-item"><strong>{data.service}</strong><span className="meta">{data.api} · v{data.version}</span></div></section><section className="lab-tools"><h2>Deployment</h2><div className="pending-item"><strong>{data.deployment.migration_ok?"Gereed":"Controle nodig"}</strong><span className="meta">DB {data.deployment.database} · migration {data.deployment.migration_revision||"onbekend"}</span></div><p className="meta">Health en database readiness zijn gecontroleerd.</p></section><section className="lab-tools"><h2>Vandaag</h2><div className="context-grid"><div><strong>{data.today.ready}</strong><p className="meta">klaar</p></div><div><strong>{data.today.active}</strong><p className="meta">actief</p></div><div><strong>{data.today.done}</strong><p className="meta">afgerond</p></div><div><strong>{data.today.blocked}</strong><p className="meta">geblokkeerd</p></div></div><p className="meta">Limiet: {data.today.important_done}/1 belangrijk · {data.today.small_done}/2 klein</p></section><section className="lab-tools"><h2>Integraties</h2>{Object.entries(data.integrations).map(([key,value])=><div className="pending-item" key={key}><strong>{key.replaceAll("_"," ")}</strong><span className="meta">{value?"geconfigureerd":"niet geconfigureerd"}</span></div>)}</section></>}{error&&<p className="message">{error}</p>}</main>}
