"use client";
import { useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function AccountSetup(){const[pw,setPw]=useState(""),[confirm,setConfirm]=useState(""),[message,setMessage]=useState(""),[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(pw!==confirm){setMessage("De wachtwoorden zijn niet gelijk.");return}const r=await fetch(`${API}/api/auth/setup`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:pw})});if(!r.ok){setMessage((await r.json()).detail||"Instellen is niet gelukt.");return}const login=await fetch(`${API}/api/auth/login`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:pw})});if(login.ok){setDone(true);setMessage("Lokale login ingesteld en sessie geopend.")}else setMessage("Login ingesteld. Log opnieuw in via Beveiliging.")}return <main className="security-shell"><header><span className="logo">ADD</span><span>Lokale login</span><a className="lab-link" href="/security">Beveiliging</a></header><section className="hero"><p className="eyebrow">BEVEILIGINGSWIZARD · STAP {done?2:1} VAN 2</p>{done?<><h1>Je bent binnen.</h1><p className="meta">Je lokale sessie is actief. ADD bewaart alleen de sterke hash.</p><a className="primary-link" href="/">Ga naar NU</a></>:<><h1>Maak je lokale login.</h1><p className="meta">ADD bewaart alleen een sterke hash. Gebruik minimaal 10 tekens.</p><form className="intake-form" onSubmit={submit}><label>Wachtwoord<input autoFocus type="password" minLength={10} value={pw} onChange={e=>setPw(e.target.value)} autoComplete="new-password"/></label><label>Herhaal wachtwoord<input type="password" minLength={10} value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password"/></label><button disabled={!pw||!confirm}>Login instellen</button></form></>}<p className="message">{message}</p></section></main>}
