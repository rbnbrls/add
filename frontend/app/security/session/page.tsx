"use client";
import { useEffect, useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export default function Session(){const[authenticated,setAuthenticated]=useState(false),[message,setMessage]=useState("Sessie controleren…");useEffect(()=>{fetch(`${API}/api/auth/status`,{credentials:"include"}).then(r=>r.json()).then(d=>{setAuthenticated(d.authenticated);setMessage(d.authenticated?"Je lokale sessie is actief.":"Je lokale sessie is vergrendeld.")})},[]);async function lock(){await fetch(`${API}/api/auth/logout`,{method:"POST",credentials:"include"});setAuthenticated(false);setMessage("ADD is vergrendeld.")}return <main className="security-shell"><header><span className="logo">ADD</span><span>Sessie</span><a className="lab-link" href="/security">Beveiliging</a></header><section className="hero"><p className="eyebrow">LOKALE SESSIE</p><h1>{authenticated?"Je bent binnen.":"Je bent vergrendeld."}</h1><p className="meta">{message}</p>{authenticated&&<button onClick={lock}>Vergrendel ADD</button>}</section></main>}
