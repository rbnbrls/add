"use client";
import { useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
const calls=[{label:"Initialize",method:"initialize"},{label:"Ping",method:"ping"},{label:"Resources list",method:"resources/list"},{label:"Prompts list",method:"prompts/list"},{label:"Decompose-template",method:"prompts/get",params:{name:"decompose_task"}},{label:"Draft-template",method:"prompts/get",params:{name:"draft_message"}},{label:"Stuck-template",method:"prompts/get",params:{name:"resolve_stuck"}},{label:"Tools list",method:"tools/list"}];
export default function MCP(){const[result,setResult]=useState("Klik een protocoltest om het JSON-RPC-resultaat te zien."),[token,setToken]=useState("");async function run(method:string,params?:Record<string,string>){const headers:Record<string,string>={"Content-Type":"application/json"};if(token)headers["X-ADD-Token"]=token;const r=await fetch(`${API}/api/mcp`,{method:"POST",headers,body:JSON.stringify({jsonrpc:"2.0",id:Date.now(),method,params:params||{}})});setResult(`${r.status} ${r.statusText}\n${await r.text()}`)}return <main><header><span className="logo">ADD</span><span>MCP test</span><a className="lab-link" href="/integrations">← Integratie Lab</a></header><section className="lab-hero"><p className="eyebrow">PROTOCOL</p><h1>Test de MCP-bridge.</h1><p className="meta">Controleer de JSON-RPC-handshake, discovery, prompttemplates en token-authenticatie vanuit de frontend.</p><div className="lab-fields"><label>API-token (optioneel)<input type="password" value={token} onChange={e=>setToken(e.target.value)} placeholder="alleen nodig als auth is ingeschakeld" autoComplete="off"/></label></div><div className="lab-actions"><button className="secondary" onClick={()=>run("ping")}>Authenticated ping</button>{calls.map(c=><button className="secondary" key={c.method+c.label} onClick={()=>run(c.method,c.params)}>{c.label}</button>)}</div></section><section className="lab-output"><h2>JSON-RPC resultaat</h2><pre>{result}</pre></section></main>}
